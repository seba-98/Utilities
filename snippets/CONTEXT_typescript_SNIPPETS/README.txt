TO USE SNIPPETS

must:

To import the snipet you press ctrl + shift + p in VSCODE 
-write user snipets
-write react typescript and press ENTER
-will open a json document
-you must copy and paste the code between the initial { }

To create context in your context file write the follow code:    ReactCtx + TAB

To create the provider react component:   ReactCtxProvider + TAB